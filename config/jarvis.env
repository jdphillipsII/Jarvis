# Central config. Source this; don't hardcode these values anywhere else.
JARVIS_AGENCY=actuator          # advisory | actuator | agentic
JARVIS_CAM=0                    # 0 = /dev/video0, or http://PHONE_IP:8080/video
JARVIS_CHAT_MODEL=qwen2.5:7b    # fast resident model: banter + intent routing
JARVIS_HEAVY_MODEL=qwen2.5:32b  # handed the hard problems
JARVIS_VOICE=en_GB-alan-medium  # Piper voice (dry British male)
